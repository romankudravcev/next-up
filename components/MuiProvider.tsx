"use client";
import React from "react";

// Placeholder provider kept for compatibility. Tailwind is used globally.
export default function MuiProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
